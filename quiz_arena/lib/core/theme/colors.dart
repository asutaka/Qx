import 'package:flutter/material.dart';

/// Defines playful, vibrant, and bright color tokens suitable for an educational/smart quiz game.
class AppColors {
  static const Color bgPrimary = Color(0xFFFFFBEB);     // Soft warm turmeric/saffron yellow
  static const Color bgSecondary = Color(0xFFFEF3C7);   // Soft amber-saffron accent
  
  static const Color accentCyan = Color(0xFF3B82F6);    // Vibrant royal blue (replaces neon cyan)
  static const Color accentPink = Color(0xFFEC4899);    // Energetic berry pink (replaces neon pink)
  static const Color accentGold = Color(0xFFF59E0B);    // Premium warm gold
  
  static const Color textPrimary = Color(0xFF1E1B4B);   // Deep indigo/navy for high text readability
  static const Color textSecondary = Color(0xFF4F46E5); // Rich purple for secondary titles/buttons
  
  static const Color cardBorder = Color(0xFFFDE68A);    // Subtle warm amber border
  static const Color cardBg = Color(0xDDFFFFFF);        // Clean translucent white card background (soft glass effect)
  
  static const Color correctGreen = Color(0xFF10B981);  // Playful emerald green
  static const Color incorrectRed = Color(0xFFEF4444);  // Coral/pastel red
}
